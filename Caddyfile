# Local/dev: plain HTTP on :80. For production, replace ":80" with your real
# domain (e.g. "bets.example.com") — Caddy will provision HTTPS automatically.
:80 {
	handle /api/* {
		reverse_proxy api:8000
	}

	handle {
		root * /srv
		file_server
		try_files {path} /index.html
	}
}
